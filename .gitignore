# Secrets / environment
.env
.env.*
!.env.example
!.env.prod.example

# Backups (never commit database dumps)
backups/

# Node
node_modules/
.next/
coverage/
*.tsbuildinfo

# Java
target/
*.class

# Test artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Runtime state
logs/
*.log

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local scratch export — not part of the platform
standalone-hotel-page/
standalone-hotel-page.zip

# Agent worktrees (isolation: "worktree") — separate checkouts, never vendor these in
.claude/worktrees/
