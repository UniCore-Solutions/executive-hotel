mutation UpdateAvailability($hotelId: ID!, $rows: [AvailabilityUpdateInput!]!) {
  updateAvailability(hotelId: $hotelId, rows: $rows) {
    id
    roomTypeId
    stayDate
    totalInventory
    roomsSold
    outOfOrder
    blocked
    free
  }
}