mutation Login($input: LoginInput!) {
  login(input: $input) {
    token
    me {
      id
      email
      roles
      hotelIds
    }
  }
}

query Me {
  me {
    id
    email
    roles
    hotelIds
  }
}