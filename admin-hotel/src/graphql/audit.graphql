query AdminAuditLogs($page: PageInput) {
  adminAuditLogs(page: $page) {
    total
    page
    size
    items {
      id
      actorUserId
      actorEmail
      action
      resourceType
      resourceId
      hotelId
      result
      metadata
      createdAt
    }
  }
}
