query AdminGuests($hotelId: ID!, $query: String, $sort: String, $page: PageInput) {
  adminGuests(hotelId: $hotelId, query: $query, sort: $sort, page: $page) {
    total
    page
    size
    items {
      id
      firstName
      lastName
      email
      phone
      countryCode
      reservationsCount
      totalSpent
      lastStayDate
    }
  }
}
