package com.hotelcollection.hotel.entity;

/** DB CHECK constraint values (chk_tax_fee_types_method). */
public enum TaxFeeCalculationMethod {
	percentage, fixed_per_night, fixed_per_stay, fixed_per_guest
}